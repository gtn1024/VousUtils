plugins {
    id "com.gradle.enterprise" version "3.6.3"
}

rootProject.name = 'VousUtils'

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
    }
}